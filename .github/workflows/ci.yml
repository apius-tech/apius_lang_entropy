name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  pr-gate:
    # Pin to @main for now; repin to a released tag (e.g. @v1) once one is cut.
    uses: apius-tech/splunk-app-ci/.github/workflows/app-ci.yml@main
    with:
      app_id: apius_lang_entropy
